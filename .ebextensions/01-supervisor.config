# Supervisor Installation
# Installs and enables Supervisor for process management

packages:
  yum:
    supervisor: []

commands:
  01-enable-supervisor:
    command: |
      # Enable supervisor to start on boot
      systemctl enable supervisord || chkconfig supervisord on
      # Ensure supervisor is running
      systemctl start supervisord || service supervisord start || true
