option_settings:
  aws:autoscaling:launchconfiguration:
    # Root volume configuration
    RootVolumeType: gp3
    RootVolumeSize: 20
    RootVolumeIOPS: 3000
    # Note: Encryption is typically enabled by default on newer instances
    # If you need encryption, configure it via EC2 Launch Template or Instance settings
