# Non-secret default environment properties.
# These are applied to the EB environment; 01-environment writes them into .env for Laravel.
# Override in Configuration → Software → Environment properties (e.g. APP_ENV=staging).
# Do not put APP_KEY, DB_*, AWS keys, or bypass magics here—use the Console or Parameter Store.

option_settings:
  aws:elasticbeanstalk:application:environment:
    APP_ENV: production
    APP_DEBUG: "false"
    LOG_LEVEL: error
